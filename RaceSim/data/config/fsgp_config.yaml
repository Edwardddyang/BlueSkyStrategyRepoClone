# For Gen 11.5
/Version: "FSGP_2025"
/Car: # Car characterizationss
  max_soc: 10.0 # kwh
  max_car_speed: 104 # kph
  max_acceleration: 2.0 # m/s^2
  max_deceleration: -2.0 # m/s^2
  tire_pressure: 5.5 # bar
  array_area: 3.98 # m^2
  car_mass: 284 # in kg
  air_density: 1.17 # in kg/m^3
  passive_electric_loss: 20 # in w
  cda: 0.12
  max_motor_power: 5.0 # in kw
  array_power_max: 950 # in W
  array_efficiency: 0.252 
  motor_efficiency: 0.9
  regen_efficiency: 0.8
  battery_efficiency: 0.95
  tire_inertia: 0.00125 # in kg * m^2
  tire_radius: 0.508 # in m
  num_tires: 3

\LutPaths:
  # All following luts are relative to gen12_strategy/RaceSim/
  base_route_path: data/luts/fsgp/static/fsgp_base_route.csv # 3 column csv --> latitude (degrees) | longitude (degrees) | altitude (km)
  power_factor_path: data/luts/fsgp/static/powerfactor.csv
  corners_path: data/luts/fsgp/static/fsgp_corners.csv # 3 Column csv -> Corner start index | Corner end index | Max speed (mps)
  roll_res_slope_path: data/luts/fsgp/static/rr2.csv
  roll_res_yint_path: data/luts/fsgp/static/rr1.csv
  dni_path: data/luts/fsgp/dynamic/dni.csv
  dhi_path: data/luts/fsgp/dynamic/dhi.csv
  wind_direction_path: data/luts/fsgp/dynamic/wind_direction_10m.csv
  wind_speed_path: data/luts/fsgp/dynamic/wind_speed_10m.csv
  precomputed_distances_path: data/luts/fsgp/static/precomputed_distances.csv # Precomputed distances between each pair of
                                                                              # coordinates in base_route_path.
                                                                              # If calculate_distances is true, it will
                                                                              # write the result to this path
  calculate_distances: False # Whether to pre-compute distances. If you're passing in a new base route never seen before,
                             # set this to true

\Route: # Route conditions
  init_control_stops: False # Use control stops or not
  day_one_start_time: 2024-08-16 10:00:00 # Start time of the first day in YYYY-MM-DD HH:MM:SS local 24 hour format
  day_one_end_time: 2024-08-16 18:00:00 # End time of the first day in YYYY-MM-DD HH:MM:SS local 24 hour format
  day_start_time: 9:00:00 # Start time from second day onwards in HH:MM:SS local 24 hour format
  day_end_time: 17:00:00 # End time from second day onwards in HH:MM:SS local 24 hour format
  race_end_time: 2024-08-18 17:00:00 # YYYY-MM-DD HH:MM:SS local 24 hour time
  impounding_release_time: 07:00:00 # HH:MM:SS of the morning battery release time
  impounding_start_time: 20:00:00 # HH:MM:SS of the nightly battery impounding time
  overnight_charging_location: 37.000823,-86.367676,0.157135 # Coordinates of the charging location
  max_route_speed: 108 # kph - comes from FSGP regulations

\Telemetry: # Current state of the car
  current_soc: 9.2 # in kWh, should be <= max_soc
  gps_coordinates: 37.0011542,-86.3683294,0.157135 # Lat(degrees),Lon(degrees),Alt(metres)
  current_date_time: 2024-08-16 10:00:00 # YYYY-MM-DD HH:MM:SS local 24 hour time
  utc_adjustment: +6.0 # Hour conversion from local time to UTC. This will be applied to all times. 
                       # This parameter MUST be present

\Optimizer:
  model: Gen 12 # Force model of the car to use
  optimizer: Acceleration # type of optimization
  threads: 0.6 # Fractional number of machine threads to use
  ####### The below parameters are for segmenting the route into initial race plans #######
  acceleration_power_budget: 0.8 # Percentage of maximum motor power that can be allocated towards acceleration
  corner_speed_min: 0.4 # Unitless fraction in range [0,1]
  corner_speed_max: 0.9 # Unitless fraction in range (corner_speed_min,1]
  aggressive_straight_threshold: 250.0 # m. Straights longer than this can be taken agressively
  num_repetitions: 5 # Number of times to repeat the plan for a single loop
  min_acceleration: 0.1 # Minimum acceleration that can be considered in m/s^2
  average_speed: 20.0 # Average speed to try and maintain throughout the route. The profile creator
                      # uses this value as the mean of a gaussian distribution to sample for
  fix_loops: true # Fix the number of loops to create. If true, then uses max_num_loops
  max_num_loops: 60 # Maximum number of loops that can be completed
  log_segmenting: false # Log the race plan creation
  fix_seeds: false # Fix seeds for route segmentation. If true, use the seeds below
  loop_seed: 3888189472
  speed_seed: 958024739
  aggressive_seed: 2358747961
  acceleration_seed: 1708337047
  idx_seed: 477365342
  skip_seed: 2880176778
  ####### Genetic Algorithm Parameters #######
  num_generations: 1  # Number of iterations
  population_size: 50  # Number of race plans to maintain the in poulation
  survival_percentage: 0  # % of the population to pass onto the next iteration unaltered
  crossover_percentage: 0  # Top % of the population to use to crossover to create new children
  mutation_percentage: 100  # % of the population to mutate outside of the ones that are being crossed over
  # Note that survival_percentage + parents_percentage + mutation_percentage must be 100
  mutation_strategy: PreferConstantSpeed

  # POSSIBLE MUTATION IDEAS:
  # PreferConstantSpeed, PreferConstantSpeedAggressive (Avoid deceleration by lowering the constant speed for the corner before a corner with lower max speed),
  # DecreaseCornerSpeed (Decrease acceleration by lowering cornering speeds)
  threads: 0.5

\Simulation:
  simulator: FSGP # Type of simulator
  save_csv: true # Save simulation results to CSVs
